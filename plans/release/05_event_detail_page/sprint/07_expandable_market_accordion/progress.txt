# Sprint 07: Expandable Market Accordion

## Overview
Implement Polymarket-style expandable market rows where each market in an event
can be clicked to expand inline, showing Order Book, Graph, and Resolution tabs.
This replaces the simple OutcomesList with an interactive accordion pattern.

## Reference
- Polymarket event page: https://polymarket.com/event/who-will-trump-nominate-as-fed-chair
- Key UX: Click on candidate row -> expands with Order Book | Graph | Resolution tabs

## Dependencies
- Sprint 00: Proxy layer (useOrderbook, useTimeseries, useMarketPrice hooks)
- Sprint 04: PriceChart, OrderBook, TimeRangeSelector components
- Sprint 02: ResolutionRules, OutcomeCard components

## Progress

### market_accordion_container
Status: COMPLETE
Files: web/src/components/event/market-accordion.tsx
Completed: 2026-02-08

Implementation notes:
- Created MarketAccordion component using @radix-ui/react-accordion
- Supports 'single' (one at a time) and 'multiple' expansion modes
- URL hash persistence for deep linking (#market-{id} or #markets-{id1},{id2})
- Smooth height animation via CSS keyframes in index.css
- Keyboard navigation handled by Radix Accordion (arrows, enter, space, home, end)
- Exported types: MarketAccordionProps, AccordionMarket, MarketOutcome, AccordionMode
- Basic placeholder content in expanded state (tabs TBD in market_accordion_content)

### market_accordion_header
Status: COMPLETE
Files: web/src/components/event/market-accordion.tsx
Completed: 2026-02-08

Implementation notes:
- Created MarketAccordionHeader component with enhanced visual design
- Large probability display with color-coded background (getProbabilityColor)
- Bid/ask spread shown inline in "Xc / Yc" format from bestBid/bestAsk
- 24h volume and liquidity indicators with formatted numbers
- PriceChangeIndicator component with TrendingUp/TrendingDown icons
- Optional profile image/avatar support via imageUrl prop
- Hover states: subtle bg-gray-50/80, shadow-md on item hover
- Expanded state: bg-gray-50/50 for visual feedback
- Added bestBid/bestAsk fields to MarketOutcome interface
- Added imageUrl field to AccordionMarket interface
- Memoized components (memo) for performance

### market_accordion_content
Status: COMPLETE
Files: web/src/components/event/market-accordion.tsx
Completed: 2026-02-08

Implementation notes:
- Created MarketAccordionContent component with Radix Tabs integration
- Three tabs: Order Book (BookOpen icon), Graph (LineChart icon), Resolution (FileText icon)
- Local state for active tab within each accordion item (not URL persisted per accordion)
- Smooth fade-in animation using Tailwind animate-in utilities
- Tab content is lazy-loaded (only rendered when accordion is expanded)
- OrderBookTabContent wrapper integrates OrderBook component with proper sizing
- GraphTabContent wrapper includes TimeRangeSelector with local state
- ResolutionTabContent wrapper integrates ResolutionRules component
- Added resolution-related fields to AccordionMarket interface
- Exported AccordionTabValue type for external use
- All tab content components memoized for performance

### orderbook_tab_integration
Status: COMPLETE
Files: web/src/components/event/market-accordion.tsx
Completed: 2026-02-09

Implementation notes:
- OrderBookTabContent manages manual refresh state
- New OrderBookWithRefresh component uses useOrderbook hook directly
- Displays last updated timestamp and "Updating..." during background refetch
- Refresh button with spinning animation during refresh
- Graceful error state showing "Order book unavailable" with retry button
- Empty state with BookOpen icon and refresh link

### graph_tab_integration
Status: NOT STARTED
Files: web/src/components/event/market-accordion.tsx
Notes: Basic integration complete in MarketAccordionContent. Remaining: sync time range option across markets.

### resolution_tab_integration
Status: COMPLETE
Files: web/src/components/event/market-accordion.tsx, web/src/components/event/resolution-rules.tsx
Completed: 2026-02-09

Implementation notes:
- Added EndDateDisplay component to ResolutionRules with Calendar/Clock icons
- Shows formatted date + relative time ("in 3 days", "2 hours ago")
- Color-coded: indigo (upcoming), amber (overdue), gray (resolved)
- Added endDate field to AccordionMarket and ResolutionRulesProps
- Passes endDate through accordion -> ResolutionTabContent -> ResolutionRules

### multi_market_layout
Status: COMPLETE
Files: web/src/components/event/market-accordion.tsx, web/src/app/(dashboard)/event/[slug]/page.tsx
Completed: 2026-02-08

### buy_buttons_inline
Status: COMPLETE
Files: web/src/components/event/market-accordion.tsx
Completed: 2026-02-08

Implementation notes:
- Added BuyButton component as compact pill buttons (green for Yes, red for No)
- Shows current price in cents (e.g., "Yes 95¢")
- Hover tooltip shows estimated cost for 100 shares
- Buttons disabled when market.isResolved is true
- Added TradeSelection type and onTradeClick callback for parent integration
- Stops event propagation to prevent accordion toggle
- Hidden on mobile (< sm) to keep header clean

### accordion_loading_states
Status: COMPLETE
Files: web/src/components/event/market-accordion.tsx
Completed: 2026-02-08

Implementation notes:
- Created MarketAccordionSkeleton component for initial page load
- SkeletonBlock with shimmer animation matching existing loading skeleton pattern
- MarketAccordionRowSkeleton with staggered animation delays
- MarketAccordionError for full error state with retry button
- MarketAccordionItemError for single market failure within a list
- MarketAccordionLoading for partial loading states (mixed loaded/loading/failed)
- All components use data-testid for testing
- Maintains accordion structure during loading to prevent layout shift
- Icons: AlertCircle, RefreshCw from lucide-react

### accordion_mobile_responsive
Status: COMPLETE
Files: web/src/components/event/market-accordion.tsx, web/src/index.css
Completed: 2026-02-08

Implementation notes:
- Stacked vertical layout on mobile (< 640px) with reduced padding
- Smaller font sizes and avatar on mobile for compact view
- Tabs scrollable horizontally with hidden scrollbar
- All touch targets meet 44px minimum accessibility requirement
- Buy buttons show abbreviated labels (Y/N) on mobile
- Secondary info (bid/ask, liquidity) hidden on mobile
- Added active states for touch feedback

### event_page_integration
Status: COMPLETE
Files: web/src/app/(dashboard)/event/[slug]/page.tsx
Completed: 2026-02-08

Implementation notes:
- Replaced OutcomesList component with MarketAccordion
- Created mapToAccordionMarkets function to transform EventMarketResponse to AccordionMarket
- Maps all market fields: id, question, outcomes, totalVolume, liquidity, volume24h, imageUrl, description, isResolved
- Maps outcome fields: id, tokenId, outcomeName, currentPrice, priceChange24h
- Uses 'single' mode (only one market expanded at a time)
- URL hash persistence enabled for deep linking
- Removed unused imports and OutcomesList component
- Typecheck and all 550 tests pass

### realtime_price_updates
Status: NOT STARTED
Files: web/src/components/event/market-accordion.tsx

## Notes
- Uses Radix Accordion for accessible expand/collapse with keyboard support
- Lazy loads tab content to minimize API calls for collapsed markets
- Supports both binary (Yes/No) and multi-outcome events
- Integrates with existing trading panel for seamless trade execution

## Session Log

### 2026-02-08: market_accordion_container
Implemented the core MarketAccordion component with:
- Radix Accordion integration for accessible expand/collapse
- Single and multiple expansion modes via `mode` prop
- URL hash synchronization for deep linking
- CSS animations added to index.css using Tailwind v4 @theme directive
- Component exported from components/event/index.ts

Next priority should be `market_accordion_header` to add proper header styling with
probability display, bid/ask spread, and volume indicators. After that,
`market_accordion_content` to implement the tabbed interface for Order Book, Graph,
and Resolution views.

### 2026-02-08: market_accordion_header
Implemented enhanced MarketAccordionHeader component with:
- Large probability display with color-coded backgrounds using getProbabilityColor
- Bid/ask spread inline display (reads from bestBid/bestAsk on MarketOutcome)
- 24h volume and liquidity indicators with abbreviated formatting
- PriceChangeIndicator with TrendingUp/TrendingDown lucide icons
- Optional candidate avatar/image support via imageUrl on AccordionMarket
- Hover/selected state feedback with subtle background changes
- Chevron rotation animation on expand/collapse
- All header components memoized for performance

Updated types:
- MarketOutcome: added bestBid, bestAsk optional fields
- AccordionMarket: added imageUrl optional field

Next priority should be `market_accordion_content` to implement the tabbed interface
with Order Book, Graph, and Resolution tabs. After that, `buy_buttons_inline` for
quick trading from the header.

### 2026-02-08: market_accordion_content
Implemented the tabbed interface for expanded accordion content with:
- MarketAccordionContent component using @radix-ui/react-tabs
- Three tabs with icons: Order Book (BookOpen), Graph (LineChart), Resolution (FileText)
- Local state for active tab (doesn't pollute URL with per-market tab state)
- Smooth fade-in animation with Tailwind animate-in utilities
- Lazy loading via conditional rendering in Accordion.Content

Tab wrapper components:
- OrderBookTabContent: wraps OrderBook component, handles empty tokenId gracefully
- GraphTabContent: includes TimeRangeSelector with local state, defaults to 1D
- ResolutionTabContent: wraps ResolutionRules component

Updated types:
- AccordionMarket: added description, resolverType, resolutionSource, isResolved, resolutionOutcome
- AccordionTabValue: new exported type for tab values

Next priority should be `buy_buttons_inline` to add quick Buy Yes/No buttons to the
accordion header for faster trading. After that, `event_page_integration` to wire
up the accordion on the actual event page.

### 2026-02-08: event_page_integration
Integrated MarketAccordion into the event detail page, replacing the simple OutcomesList:

Changes to web/src/app/(dashboard)/event/[slug]/page.tsx:
- Imported MarketAccordion, AccordionMarket, and MarketOutcome types from @/components/event
- Created mapToAccordionMarkets() function to transform API EventMarketResponse to AccordionMarket
- Removed OutcomesList component (no longer needed)
- Replaced <OutcomesList markets={...} /> with <MarketAccordion markets={...} mode="single" persistToUrl />
- Cleaned up unused imports (TrendingUp, TrendingDown, BarChart2)
- Removed helper functions (formatPrice, formatPriceChange, formatCurrency) that were only used by OutcomesList

The accordion now shows on the event page with:
- Clickable market rows that expand to show Order Book, Graph, and Resolution tabs
- Single expansion mode (only one market open at a time)
- URL hash persistence for deep linking to specific markets
- All market data passed through: outcomes, prices, volume, liquidity, images, resolution info

Next priority should be `buy_buttons_inline` to add quick Buy Yes/No buttons to the
accordion header for faster trading. Consider also `accordion_loading_states` for
better loading UX and `accordion_mobile_responsive` for mobile optimization.

### 2026-02-08: buy_buttons_inline
Implemented inline Buy Yes/No buttons in the accordion header for quick trading:

Changes to web/src/components/event/market-accordion.tsx:
- Added BuyButton component as a compact pill button with green (Yes) and red (No) styling
- Integrated buttons into MarketAccordionHeader on the right side, hidden on mobile (< sm breakpoint)
- Shows current price on each button in cents format (e.g., "Yes 95¢")
- Added TradeSelection interface for structured callback data
- Added onTradeClick callback prop to MarketAccordion for parent component integration
- Buttons use stopPropagation to prevent accordion toggle when clicked
- Hover tooltip shows estimated cost for 100 shares ("Buy 100 shares for $X")
- Buttons are disabled when market.isResolved is true
- All components memoized for performance (BuyButton wrapped in memo)
- Added allOutcomes prop to MarketAccordionHeader for accessing Yes/No outcomes
- Finds Yes/No outcomes by matching outcomeName.toLowerCase()

Updated exports in web/src/components/event/index.ts:
- Added TradeSelection type export

Features implemented:
- Compact pill buttons with appropriate colors (green-100/700 for Yes, red-100/700 for No)
- Price display in cents with tabular-nums for consistent width
- Proper focus states with ring-2 and appropriate color
- Hover states with shadow-sm and color intensity increase
- Disabled state with gray styling and cursor-not-allowed
- Responsive: hidden on mobile to avoid header crowding

Parent integration notes:
- Parent can pass onTradeClick callback to receive TradeSelection with:
  - marketId, outcomeId, tokenId, outcomeName, currentPrice, direction
- This enables wiring to TradingPanel to pre-select the outcome for trading

Typecheck and all 550 tests pass.

Next priority should be `accordion_loading_states` for better loading UX, or
`accordion_mobile_responsive` to ensure buttons work well on all screen sizes.

### 2026-02-08: accordion_loading_states
Implemented comprehensive loading and error states for the market accordion:

New components added to web/src/components/event/market-accordion.tsx:
- SkeletonBlock: Base shimmer animation block reusing existing pattern
- MarketAccordionRowSkeleton: Single row skeleton matching accordion header structure
- MarketAccordionSkeleton: Multiple skeleton rows for initial loading state
- MarketAccordionError: Full error state with icon, message, and retry button
- MarketAccordionItemError: Single market error state for partial failures
- MarketAccordionLoading: Orchestrates mixed states (loaded, loading, failed markets)

Features:
- Skeleton rows match accordion header structure exactly (avatar, question, stats, buttons, probability)
- Staggered animation delays for visual interest
- Red-themed error states with AlertCircle icon
- Retry buttons using RefreshCw icon
- MarketAccordionLoading handles three scenarios:
  1. All loading -> shows full skeleton
  2. All failed -> shows error state with retry
  3. Mixed -> shows loaded markets + skeleton rows + error items
- All components use data-testid for testing
- Maintains layout dimensions to prevent CLS (Cumulative Layout Shift)

Updated exports in web/src/components/event/index.ts:
- MarketAccordionSkeleton, MarketAccordionError, MarketAccordionItemError, MarketAccordionLoading
- Corresponding Props types for all new components

Typecheck and all 550 tests pass.

Next priority should be `accordion_mobile_responsive` to optimize the accordion
for mobile viewports, or `multi_market_layout` to handle events with many markets.

### 2026-02-08: multi_market_layout
Implemented the MultiMarketAccordion component for handling events with many markets:

New component added to web/src/components/event/market-accordion.tsx:
- MultiMarketAccordion: wrapper component for handling 10+ market events

Features implemented:
- Support for events with 10+ markets (like Fed Chair nominees)
- "Show all X markets" expandable section with show more/less buttons
- Display top N markets initially (configurable via initialDisplayCount, default: 5)
- Search/filter input for finding specific markets (searches question and outcome names)
- Sort dropdown with three options: Probability (default), Volume, Alphabetical
- "X more markets" indicator when list is collapsed
- Market count display showing "X of Y markets" when filtered
- No results state with clear search button
- Auto-show all results when searching (to not hide matches)

Helper functions added:
- getPrimaryProbability: gets highest outcome price for sorting
- sortMarkets: sorts by probability (desc), volume (desc), or alphabetical
- filterMarkets: filters by search query in question or outcome names

Sub-components:
- MultiMarketControls: search input and sort dropdown with market count
- ShowMoreButton: dashed border button showing hidden count
- ShowLessButton: collapse button to return to initial display count

Updated exports in web/src/components/event/index.ts:
- MultiMarketAccordion, MultiMarketAccordionProps, MarketSortOption

Updated event page (web/src/app/(dashboard)/event/[slug]/page.tsx):
- Replaced MarketAccordion with MultiMarketAccordion
- Set initialDisplayCount=5 and defaultSort="probability"

Typecheck and all 550 tests pass.

Next priority should be `accordion_mobile_responsive` to optimize the accordion
for mobile viewports, or one of the remaining tab integration improvements.

### 2026-02-08: accordion_mobile_responsive
Implemented comprehensive mobile responsive optimizations for the market accordion:

Changes to web/src/components/event/market-accordion.tsx:

MarketAccordionHeader:
- Refactored to use responsive flex layout (column on mobile, row on desktop)
- Mobile layout stacks content vertically with reduced padding (p-3 vs p-4)
- Top row shows: avatar (8x8 vs 10x10), question (text-sm vs text-base), probability badge, chevron
- Bottom row shows: price change indicator, volume, and Buy Yes/No buttons
- Secondary info (bid/ask, liquidity) hidden on mobile to reduce clutter
- Probability display uses smaller format on mobile (text-lg vs text-2xl)

BuyButton:
- Added `size` prop ('sm' | 'default') for responsive sizing
- Small size: reduced padding (px-2 py-1.5), smaller text (11px), min-h-[32px]
- Small buttons show abbreviated labels ("Y" / "N" vs "Yes" / "No")
- Added active states (active:bg-green-300/red-300) for touch feedback

PriceChangeIndicator:
- Added `size` prop ('sm' | 'default') for responsive sizing
- Small size: reduced padding, smaller text (11px), smaller icon (2.5x2.5)

MarketAccordionContent (tabs):
- Tab list is horizontally scrollable with scrollbar-hide CSS utility
- Tab triggers have min-h-[44px] on mobile for proper touch targets
- Reduced padding on mobile (px-3 py-3 vs px-4 py-2.5)
- Tab content container uses reduced padding on mobile (p-3 vs p-4)

MarketAccordionRowSkeleton:
- Separate mobile and desktop layouts for skeleton loading state
- Mobile skeleton matches new stacked layout structure
- Desktop skeleton unchanged

MultiMarketControls:
- Search input is full-width on mobile (no max-w-sm)
- Taller input on mobile (py-2.5) for better touch target
- Clear button has larger touch target (p-2)
- Sort button has min-h-[44px] on mobile
- Sort dropdown menu items have larger touch targets (py-3 vs py-2)
- Count text shows abbreviated format on mobile ("X of Y" vs "X of Y markets")

ShowMoreButton / ShowLessButton:
- Increased vertical padding on mobile (py-3.5 vs py-3)
- Added active states for touch feedback

CSS additions (web/src/index.css):
- Added .scrollbar-hide utility class for hiding scrollbars while allowing scroll
- Works across all browsers (Firefox, Chrome/Safari/Opera, IE/Edge)

Accessibility notes:
- All interactive elements have minimum 44px touch target height on mobile
- Tab navigation fully keyboard accessible via Radix primitives
- Focus states maintained for all interactive elements

Typecheck and all 550 tests pass.

Remaining items:
- orderbook_tab_integration: needs refresh button
- graph_tab_integration: needs sync time range option
- resolution_tab_integration: needs end date display

### 2026-02-09: realtime_price_updates
Implemented real-time price updates for the market accordion headers:

New hook added to web/src/hooks/use-polymarket-proxy.ts:
- useRealtimePrices: Fetches real-time prices for multiple markets in parallel
- Uses React Query's useQueries for efficient parallel fetching
- Supports configurable polling interval (default 10s)
- Supports pauseWhenCollapsed optimization to reduce API calls
- Returns a map of conditionId -> MarketPriceUpdate with tokens, volume24h, liquidity, lastUpdated

New types exported from hooks:
- MarketPriceUpdate: Interface for price update data
- UseRealtimePricesParams: Configuration for the hook

CSS animations added to web/src/index.css:
- price-flash-up: Green flash animation for price increases
- price-flash-down: Red flash animation for price decreases
- Animations use @theme directive for Tailwind v4 integration

New component added to web/src/components/event/market-accordion.tsx:
- AnimatedPriceDisplay: Shows probability with flash animation on price changes
- Tracks previous price via usePreviousValue hook to detect changes
- Flashes green/red when price changes by >= 0.1%
- Shows last update timestamp on hover via title attribute
- Supports 'sm' and 'default' size variants

Updates to MarketAccordion component:
- Added RealtimePriceConfig interface for configuration
- Added realtimePrices prop to MarketAccordionProps and MultiMarketAccordionProps
- Integrated useRealtimePrices hook when enabled
- Merges real-time prices into markets data
- Passes expandedIds to hook for pauseWhenCollapsed optimization
- Updated MarketAccordionHeader to use AnimatedPriceDisplay

Updates to event page (web/src/app/(dashboard)/event/[slug]/page.tsx):
- Enabled real-time prices with 10s interval and pauseWhenCollapsed=true

Features implemented per PRD:
1. Uses useRealtimePrices (built on useMarketPrice) to poll for price updates (10s interval) ✓
2. Animate price changes with brief highlight (flash green/red) ✓
3. Update probability display without full re-render (via memo and specific updates) ✓
4. Pause polling when accordion is collapsed (optimization) ✓
5. Resume polling when accordion expands ✓
6. Show last update timestamp on hover ✓

Typecheck and all 550 tests pass.

Remaining items:
- graph_tab_integration: needs sync time range option
- resolution_tab_integration: needs end date display

### 2026-02-09: orderbook_tab_integration
Completed the orderbook tab integration with refresh button and improved error handling:

Changes to web/src/components/event/market-accordion.tsx:

Updated OrderBookTabContent component:
- Added isManualRefreshing state for tracking manual refresh operations
- Created handleRefresh callback that sets refresh state and awaits refetch

New OrderBookWithRefresh component:
- Separated component to use useOrderbook hook directly with refetch capability
- Displays last updated timestamp (e.g., "Updated 2:30:15 PM")
- Shows "Updating..." indicator during background refetches
- Refresh button with spinning icon animation during refresh
- Button shows "Refresh" text on desktop, icon-only on mobile

Error handling improvements:
- Graceful error state with AlertCircle icon and friendly message
- Shows "Order book unavailable" instead of raw error text
- Error details displayed in smaller text below
- Retry button to attempt refetch

Empty state improvements:
- Shows BookOpen icon when no orders available
- Provides refresh link to manually retry

Updated imports:
- Added OrderBookSkeleton import from order-book component
- Added useOrderbook import from hooks

Features implemented per PRD:
1. Create OrderBookTab wrapper component ✓
2. Pass tokenId from expanded market to useOrderbook hook ✓
3. Display OrderBook component with proper sizing for accordion context ✓
4. Show OrderBookSkeleton while data is loading ✓
5. Handle orderbook unavailable gracefully (show message, not error) ✓
6. Add refresh button for manual orderbook update ✓

Typecheck and all 550 tests pass.

Remaining items:
- graph_tab_integration: needs sync time range option

### 2026-02-09: resolution_tab_integration
Completed the resolution tab integration with prominent end date display:

Changes to web/src/components/event/resolution-rules.tsx:

New EndDateDisplay component:
- Shows resolution deadline prominently with Calendar icon (upcoming) or Clock icon (past)
- Displays both formatted date ("Jan 15, 2025 at 11:59 PM") and relative time ("in 3 days")
- Color-coded based on status:
  - Indigo theme for upcoming deadlines (active markets)
  - Amber theme for overdue deadlines (past but not resolved)
  - Gray theme for ended markets (already resolved)
- Shows "Resolution Deadline" for active, "Resolution Overdue" for past, "Ended" for resolved

Added formatEndDate helper function:
- Parses ISO 8601 date strings
- Calculates relative time in minutes/hours/days/months
- Returns formatted string, relative time description, and isPast boolean

Updated ResolutionRulesProps interface:
- Added endDate?: string | null for market resolution deadline

Updated ResolutionRules component:
- Added endDate prop to function parameters
- Renders EndDateDisplay below resolved status banner when endDate is present

Changes to web/src/components/event/market-accordion.tsx:

Updated AccordionMarket interface:
- Added endDate?: string for market resolution deadline

Updated ResolutionTabContent:
- Passes market.endDate to ResolutionRules component

Changes to web/src/app/(dashboard)/event/[slug]/page.tsx:

Updated mapToAccordionMarkets function:
- Maps market.endDate to AccordionMarket.endDate

Features implemented per PRD:
1. Create ResolutionTab wrapper component ✓ (already existed)
2. Display market-specific resolution criteria ✓ (already existed)
3. Show resolution source and resolver information ✓ (already existed)
4. Parse and render markdown description with proper formatting ✓ (already existed)
5. Add collapsible section for long resolution rules ✓ (already existed)
6. Include end date and resolution deadline prominently ✓ (NEW)

Typecheck and all 550 tests pass.

### 2026-02-09: graph_tab_integration
Completed the graph tab integration with configurable time range sync feature:

New interface added to web/src/components/event/market-accordion.tsx:
- TimeRangeSyncConfig: Configuration for syncing time range across markets
  - enabled?: boolean (default: false = independent time ranges)
  - defaultRange?: TimeRange (default: 1D when sync is enabled)

Changes to MarketAccordionProps:
- Added timeRangeSync?: TimeRangeSyncConfig prop

Changes to MultiMarketAccordionProps:
- Added timeRangeSync?: TimeRangeSyncConfig prop (passes through to MarketAccordion)

Changes to MarketAccordion component:
- Added sharedTimeRange state for synced mode
- Passes sync configuration down through MarketAccordionItem to MarketAccordionContent

Changes to MarketAccordionItemProps:
- Added syncTimeRange?: boolean
- Added sharedTimeRange?: TimeRange
- Added onTimeRangeChange?: (range: TimeRange) => void

Changes to MarketAccordionContent:
- Supports both local and shared time range modes
- When syncTimeRange is enabled, uses sharedTimeRange and calls parent onTimeRangeChange
- When syncTimeRange is disabled (default), uses local state per accordion item

Updated exports in web/src/components/event/index.ts:
- Added TimeRangeSyncConfig type export

Features implemented per PRD:
1. Create GraphTab wrapper component ✓ (already existed)
2. Pass tokenId from expanded market to useTimeseries hook ✓ (already existed)
3. Include TimeRangeSelector for 1H/6H/1D/1W/1M/ALL options ✓ (already existed)
4. Display PriceChart component with responsive height for accordion ✓ (already existed)
5. Show ChartLoadingSkeleton while data is loading ✓ (already existed)
6. Default to 1D range for initial view ✓ (already existed)
7. Sync time range across all markets or keep independent (configurable) ✓ (NEW)

Usage example for enabling sync mode:
```tsx
<MultiMarketAccordion
  markets={markets}
  timeRangeSync={{
    enabled: true,
    defaultRange: '1D',
  }}
/>
```

Typecheck and all 550 tests pass.

## Sprint 07 Complete

All categories in the PRD now pass:
- market_accordion_container ✓
- market_accordion_header ✓
- market_accordion_content ✓
- orderbook_tab_integration ✓
- graph_tab_integration ✓
- resolution_tab_integration ✓
- multi_market_layout ✓
- buy_buttons_inline ✓
- accordion_loading_states ✓
- accordion_mobile_responsive ✓
- event_page_integration ✓
- realtime_price_updates ✓