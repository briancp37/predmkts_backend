# Sprint Progress: Data Visualization
# Release: 05_event_detail_page
# Created: 2026-02-06

## Status: In Progress

## Completed Tasks

### 2026-02-06: price_chart_component
- Created `web/src/components/event/price-chart.tsx` using Recharts
- Uses `useTimeseries` hook from `use-polymarket-proxy.ts` for data fetching
- Implemented AreaChart with gradient fill (indigo color scheme)
- Y-axis formatted as percentage (0-100%) with dynamic domain based on price range
- X-axis with smart time formatting per interval (1m, 5m, 1h, 1d, 1w, max)
- Custom tooltip showing date/time, price as percentage, and price as cents
- Crosshair cursor on hover
- 50% reference line for quick probability reading
- Loading skeleton, empty state, and error state with retry button
- Exported from `web/src/components/event/index.ts`
- Tests pass (82 tests)

### 2026-02-06: chart_time_range_selector
- Created `web/src/components/event/time-range-selector.tsx`
- Button group with 1H, 6H, 1D, 1W, 1M, ALL options
- Maps user-friendly ranges to Polymarket API intervals:
  - 1H → 1m, 6H → 5m, 1D → 15m, 1W → 1h, 1M → 6h, ALL → max
- Selected range highlighted with indigo accent color (consistent with other components)
- Supports both controlled and uncontrolled modes
- URL query param persistence via `?range=1W` (uses Next.js useSearchParams)
- Loading indicator on selected button during data fetch (Loader2 spinner)
- Helper hooks: `useTimeRangeFromUrl()` and `useTimeseriesIntervalFromUrl()`
- Exported from `web/src/components/event/index.ts`
- Typecheck passes, all 118 tests pass

### 2026-02-06: chart_tooltip
- Created `web/src/components/event/chart-tooltip.tsx` as separate reusable component
- Styled container with rounded corners, shadow, and border
- Displays date (MMM d, yyyy) and time (HH:mm) on separate lines for clarity
- Shows price as both percentage (e.g., "65.5%") and cents (e.g., "66¢")
- Shows outcome name when provided (e.g., "Yes")
- **Price change from previous period**: Calculates and displays absolute change (in cents)
  and percentage change with directional indicator (green up arrow, red down arrow, gray minus)
- Uses lucide-react icons (ArrowUp, ArrowDown, Minus) for change direction
- Smart edge positioning: Detects proximity to chart edges and adjusts tooltip position
- Updated `price-chart.tsx` to include `previousPrice` in chart data for change calculation
- Exported `ChartTooltip`, `ChartTooltipProps`, `ChartDataPoint` from `index.ts`
- **Note**: OHLC (Open, High, Low, Close) and volume data not available from Polymarket
  timeseries API (which returns only simple `{t, p}` points). The tooltip focuses on what's
  available: current price and change from previous period. If OHLC data becomes available
  in the future, the component can be extended.
- Typecheck passes, all 175 tests pass

### 2026-02-06: chart_loading_states
- Created `web/src/components/event/chart-loading-states.tsx` with reusable loading state components:
  - `ChartLoadingSkeleton`: Animated placeholder for initial chart loading with configurable height
  - `ChartEmpty`: "No data available" state with customizable message
  - `ChartError`: Error state with retry button and accessible focus styles
  - `ChartRefetchIndicator`: Subtle overlay indicator for data refetch (range change)
  - `ChartContainer`: Convenience wrapper with refetch indicator positioning
- Updated `price-chart.tsx` to use new reusable components:
  - Replaced inline loading states with imports from `chart-loading-states.tsx`
  - Added `isFetching` from useTimeseries hook to detect refetch vs initial load
  - Shows `ChartRefetchIndicator` overlay when fetching but not loading (i.e., on range change)
  - Maintains chart visibility during refetch with subtle "Updating..." indicator
- All components maintain chart dimensions via `style={{ height }}` to prevent layout shift
- Exported all components and their prop types from `web/src/components/event/index.ts`
- Typecheck passes, all 175 tests pass

### 2026-02-06: orderbook_component
- Created `web/src/components/event/order-book.tsx` using useOrderbook hook
- Uses `useOrderbook` hook from `use-polymarket-proxy.ts` for real-time L2 data
- Horizontal cumulative depth bars showing total size at each price level
- Bar width scaled relative to maximum cumulative size across both sides
- **Best bid/ask prominently displayed** in centered spread row between asks and bids
- **Spread calculation**: Shows spread in cents (e.g., "2.0¢") and percentage (e.g., "3.08%")
- Color coding: Green for bids (buy orders), Red for asks (sell orders)
- Configurable levels prop (default: 5 levels per side)
- Three-column layout: Price | Size | Total (cumulative)
- Loading, empty, and error states with retry button:
  - `OrderBookSkeleton`: Animated placeholder matching layout
  - `OrderBookEmpty`: "Order book unavailable" message
  - `OrderBookError`: Error with retry callback
- Displays additional market info: Mid price, tick size, min order size
- Exported `OrderBook`, `OrderBookSkeleton`, `OrderBookError`, `OrderBookEmpty` from `index.ts`
- Typecheck passes, all 237 tests pass

### 2026-02-06: orderbook_loading_states
- Extended `useOrderbook` hook in `use-polymarket-proxy.ts`:
  - Added exponential backoff retry via `retryDelay` option (1s, 2s, 4s, 8s max)
  - Configurable `retryCount` parameter (default: 3 retries)
  - Hook now returns `dataUpdatedAt` for stale detection
- Created `OrderBookStaleIndicator` component in `order-book.tsx`:
  - Shows amber warning icon + "Stale" text when data is older than 10 seconds
  - Uses `dataUpdatedAt` from useOrderbook to detect staleness
  - Displayed in header next to "Order Book" title
  - Tooltip explains "Order book data may be outdated"
- `OrderBookSkeleton`, `OrderBookEmpty`, `OrderBookError` were already implemented in
  orderbook_component task
- Refetch interval was already configurable (default 5s) in useOrderbook
- Exported `OrderBookStaleIndicator` and its props from `index.ts`
- Typecheck passes, 273 tests pass (11 pre-existing failures in market-details-panel.test.tsx
  due to timezone and timeout issues unrelated to this task)

### 2026-02-06: orderbook_depth_visual
- Extended `web/src/components/event/order-book.tsx` with depth visualization features:
- **Hover state with tooltip** (`OrderLevelTooltip` component):
  - Shows detailed info when hovering over any order level row
  - Displays: Side label (Bid/Ask), Price (cents and %), Size (shares), Cumulative (shares),
    Notional value (price × size in USD)
  - Dark tooltip with arrow pointing to the hovered row
  - Row background highlights on hover (gray-50)
- **Animated bar changes**:
  - CSS transitions (duration-300 ease-out) on depth bar width changes
  - Depth bars smoothly animate when order book data updates
  - Hover state also transitions bar color (green-100→green-200 for bids, red-100→red-200 for asks)
- **Mid-price line visual**:
  - Horizontal gradient line spanning full width between asks and bids
  - Gradient from green (bid side) through indigo (mid) to red (ask side)
  - Diamond indicator at center marks the mid-price position
  - Enhanced spread row with rounded pill container showing Bid | Mid | Ask
  - Spread value now displayed separately below the mid-price indicator
- Added `data-testid` attributes for testing: `orderbook-level-tooltip`, `orderbook-level-bid`,
  `orderbook-level-ask`, `orderbook-mid-price-section`
- Updated component header comment to reference `orderbook_depth_visual` PRD task
- Typecheck passes, all 357 tests pass

## In Progress


## Blockers/Issues

- Pre-existing TypeScript error in `event/[slug]/page.tsx` - unused `TradingPanel` import
  added by previous work. Not related to this sprint task.

## Notes

- The existing `web/src/components/ui/price-chart.tsx` uses `lightweight-charts` library.
  The new event PriceChart uses Recharts as specified in the PRD for consistency with
  other chart components (VolumeChart, etc).
- The PriceChart accepts a `tokenId` prop which comes from `EventMarketOutcome.tokenId`
  in the event response.
- TimeRangeSelector can be composed with PriceChart - use `useTimeseriesIntervalFromUrl()`
  to get the interval from URL and pass to PriceChart's `interval` prop.
- Remaining task: volume_overlay
  - volume_overlay is blocked by API limitation (no per-period volume in timeseries endpoint).
- **API limitation**: The Polymarket CLOB `/prices-history` endpoint only returns `{t, p}`
  (timestamp, price) data points. True OHLC candlestick data or per-period volume would
  require either aggregating from trade data or waiting for a candlestick API endpoint.

