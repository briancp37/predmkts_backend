# Sprint Progress: Market Display Components
# Release: 05_event_detail_page
# Created: 2026-02-06

## Status: In Progress

## Completed Tasks

### 2026-02-06: probability_display category
- Created `web/src/components/event/probability-display.tsx` with:
  - `formatProbability()` - formats 0.12 -> "12%" with configurable decimals
  - `formatProbabilityAsCents()` - formats 0.12 -> "12¢"
  - `getProbabilityColor()` - returns color classes based on probability value
    - 70-100%: green
    - 50-69%: emerald
    - 30-49%: yellow
    - 15-29%: orange
    - 0-14%: red
  - `ProbabilityBadge` - inline badge component with size variants (sm/md/lg)
  - `ProbabilityBar` - visual progress bar with animation support
  - `LargeProbabilityDisplay` - large percentage display for headers
  - `ProbabilityChange` - displays 24h change with up/down arrows
- Added 36 tests in `web/tests/probability-display.test.tsx`
- All typecheck and tests passing

### 2026-02-06: event_header category
- Created `web/src/components/event/event-header.tsx` with:
  - `formatVolume()` - formats currency with K/M suffixes (e.g., "$66.1K Vol.")
  - `formatEndDate()` - formats dates with relative time (e.g., "Ends Jun 30, 2026")
  - `getCategoryColors()` - maps category names to predefined color classes
  - `CategoryBadge` - colored badge component with predefined color mapping
    - Politics=blue, Crypto=orange, Tech=purple, Sports=green, etc.
  - `WatchlistButton` - toggle button for authenticated users with optimistic updates
  - `EventHeader` - main header component combining all elements:
    - Title with text-2xl/3xl bold typography
    - Category badge with color coding
    - Resolved badge indicator
    - Large probability display with "chance" suffix
    - 24h probability change with arrows
    - Volume display with proper formatting
    - End date with relative time
    - Watchlist toggle for authenticated users
- Exported all components and types via `web/src/components/event/index.ts`
- Added 30 tests in `web/tests/event-header.test.tsx`
- All 175 frontend tests passing

### 2026-02-06: outcome_cards category
- Created `web/src/components/event/outcome-card.tsx` with:
  - `OutcomeCardData` - type for outcome data with id, tokenId, outcomeName, currentPrice, volume, priceChange24h
  - `OutcomeCard` - individual outcome card component:
    - Displays outcome name (Yes/No or custom names)
    - Shows probability as percentage with color coding via `getProbabilityColor()`
    - Displays price in cents (e.g., "65¢")
    - Shows 24h change with up/down arrow and color (green/red)
    - Hover state with subtle background change (`hover:bg-gray-100`)
    - Clickable with selection state (indigo border/background when selected)
    - Keyboard accessible (Enter/Space to select)
    - Size variants (sm/md)
    - Memoized for performance
  - `OutcomeCardList` - renders multiple OutcomeCards:
    - Vertical or horizontal layout options
    - Handles selection state across cards
    - Empty state message when no outcomes
    - Passes size/clickable props to all cards
- Exported components and types via `web/src/components/event/index.ts`
- Added 37 tests in `web/tests/outcome-card.test.tsx`
- All 237 frontend tests passing

### 2026-02-06: market_details_panel category
- Created `web/src/components/event/market-details-panel.tsx` with:
  - `formatCurrency()` - formats USD with locale (e.g., "$66,109", "$1.50M", "$2.50B")
  - `formatFullDateTime()` - formats dates with full date and time (e.g., "Jun 30, 2026, 11:59 PM")
  - `formatRelativeTime()` - formats dates relative to now (e.g., "2 weeks ago", "3 months ago")
  - `truncateAddress()` - truncates addresses for display (e.g., "0x1234...5678")
  - `CopyButton` - copy to clipboard with checkmark feedback (2s timeout)
  - `DetailRow` - reusable row component with icon, label, value, and optional copy
  - `MarketDetailsPanel` - main sidebar panel component:
    - Total volume with currency formatting
    - Liquidity display
    - Resolution date with full date/time
    - Created date with relative time and hover tooltip for full date
    - Resolver address (truncated) with copy button
    - Market ID (truncated) with copy button
    - Expandable "Additional Details" section with:
      - Condition ID (truncated) with copy button
      - Slug (truncated if long) with copy button
    - All fields are optional and only render when provided
    - Uses Card component for consistent styling
    - Memoized components for performance
- Exported all components and helper functions via `web/src/components/event/index.ts`
- Added 47 tests in `web/tests/market-details-panel.test.tsx`
- All typecheck and tests passing

### 2026-02-06: resolution_rules category
- Created `web/src/components/event/resolution-rules.tsx` with:
  - `parseResolutionCriteria()` - parses description text to extract:
    - Main text (paragraphs)
    - Key resolution criteria (bullet points, numbered lists)
  - `shouldCollapse()` - determines if text exceeds collapse threshold (~480 chars)
  - `ResolutionCriteriaList` - displays extracted criteria in highlighted blue box
    - Uses Scale icon from Lucide
    - Renders bullet points with proper styling
  - `DescriptionText` - handles text display with truncation
    - Splits into paragraphs for proper rendering
    - Truncates at word boundaries for collapsed state
  - `ResolverInfo` - displays resolver type and source information
    - Conditional rendering when data is provided
    - Labeled display format (Resolver:, Source:)
  - `ResolutionRules` - main component with:
    - Resolved status banner (green background when resolved)
    - Description with collapsible long text (Show more/less)
    - Key Resolution Criteria section (highlighted bullet points)
    - Resolver Information section
    - Disabled "Propose Resolution" button placeholder with "coming soon" text
    - Uses Card component for consistent styling
    - All sub-components memoized for performance
- Exported `ResolutionRules` and `ResolutionRulesProps` via `web/src/components/event/index.ts`
- Added 31 tests in `web/tests/resolution-rules.test.tsx`
- All 388 frontend tests passing, typecheck clean

## In Progress


## Blockers/Issues


## Notes
- These probability components are foundational - they will be used by event_header
  and outcome_cards categories
- The color gradient uses a 5-tier system matching the existing codebase patterns
- Animation uses CSS transitions with 500ms ease-out for smooth bar fills
- Components are memoized for performance when rendering multiple outcomes
- MarketDetailsPanel uses Lucide icons for visual consistency
- Clipboard copy uses native browser API with fallback error handling
- Relative time formatting supports years/months/weeks/days/hours/minutes/just now
- Remaining categories: category_tags (partially done - CategoryBadge exists in
  event-header.tsx, needs clickable navigation and hover tooltip)

