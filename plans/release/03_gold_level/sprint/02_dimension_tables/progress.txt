# Sprint Progress: Dimension Tables
# Release: 03_gold_level
# Created: 2026-02-02

## Status: In Progress

## Completed Tasks
- [2026-02-02] canonical_id_mapping: Implemented cross-platform canonical market ID resolution
  - Created mappings/ directory at project root
  - Created mappings/market_links.yaml with initial structure (empty mappings, documented format)
  - Created src/prediction_data/gold/canonical.py with CanonicalResolver class (YAML loader + lookup)
  - Fallback: unmapped (platform, id) pairs return the platform-native ID as canonical_market_id
  - 7 unit tests in tests/gold/test_canonical.py (mapped, unmapped, multi-platform, empty, missing file)
  - Added pyyaml>=6.0.0 to project dependencies, types-PyYAML>=6.0.0 to dev dependencies
  - All 762 tests pass, mypy clean on new code (2 pre-existing warnings in silver/)

- [2026-02-02] dim_platform: Static platform dimension table
  - Created ClickHouse DDL: docker/clickhouse/init/002_dim_platform.sql (ReplacingMergeTree, ORDER BY platform_id)
  - Created src/prediction_data/gold/dimensions.py with load_dim_platform() loader
  - Static data: polymarket, kalshi (platform_id, platform_name, url)
  - Writes to S3 Gold Parquet (idempotent partition overwrite) + inserts into ClickHouse
  - Supports --dry-run, optional S3 bucket (skips S3 if not configured)
  - Added `prediction-data gold load-dims` CLI command (--table dim_platform, --dry-run)
  - 8 unit tests in tests/gold/test_dimensions.py (arrow conversion, S3+CH write, dry-run, skip-S3)
  - All 770 tests pass, mypy clean

- [2026-02-02] dim_market: Market dimension table from Silver markets with canonical IDs
  - Created ClickHouse DDL: docker/clickhouse/init/003_dim_market.sql (ReplacingMergeTree(updated_at), ORDER BY (platform, platform_market_id))
  - Schema: platform, platform_market_id, canonical_market_id, question, description, market_slug, status, event_id, tokens
  - Added load_dim_market() to src/prediction_data/gold/dimensions.py
  - Reads Silver polymarket.markets via PyIceberg, transforms to dim_market rows
  - Applies CanonicalResolver for canonical_market_id resolution (fallback to platform-native ID)
  - Writes to S3 Gold Parquet (idempotent partition overwrite) + inserts into ClickHouse
  - Supports --dry-run, optional S3 bucket, injectable catalog/resolver for testing
  - Kalshi scaffolded: DDL supports any platform, _read_silver_markets("kalshi") ready when data exists
  - Registered dim_market in CLI: `prediction-data gold load-dims --table dim_market`
  - 9 new unit tests in tests/gold/test_dimensions.py (schema, transform, canonical mapping, nulls, dry-run, S3+CH write)
  - All 779 tests pass, mypy clean

- [2026-02-02] dim_outcome: Outcome dimension derived from markets token data
  - Created ClickHouse DDL: docker/clickhouse/init/004_dim_outcome.sql (ReplacingMergeTree(version), ORDER BY (platform, market_id, outcome_id))
  - Schema: platform, market_id, outcome_id, token_id, side, outcome_label
  - Added parse_tokens() utility: parses JSON tokens string, handles None/empty/invalid JSON gracefully
  - Added _silver_to_dim_outcome() transform: reads Silver markets, explodes tokens into one row per outcome
  - Side assignment: token1 for index 0, token2 for index 1 (positional)
  - outcome_id: deterministic composite key "{market_id}_{index}"
  - Added load_dim_outcome() loader: reads Silver → transforms → writes S3 Gold Parquet + ClickHouse
  - Supports --dry-run, optional S3 bucket, injectable catalog for testing
  - Registered dim_outcome in CLI: `prediction-data gold load-dims --table dim_outcome`
  - 16 new unit tests (6 parse_tokens, 7 transform, 3 loader) in tests/gold/test_dimensions.py
  - All 795 tests pass, mypy clean

## In Progress


## Blockers/Issues


## Notes
- The default YAML path resolves relative to canonical.py's location (3 parents up = repo root).
  This works for installed and editable installs but would need adjustment if the package
  were distributed as a standalone wheel without the mappings/ directory.
- Next priority: dim_wallet (scan Silver trades for distinct wallet addresses).
- The load-dims CLI is extensible: LOADABLE_DIMS tuple in gold.py controls which tables are available.
  Add new loaders to dimensions.py and register them in the CLI dispatch.
- dim_market uses CanonicalResolver — ensure mappings/market_links.yaml is populated before
  production runs if cross-platform canonical IDs are needed.
- The ClickHouse insert for dim_market passes dicts via to_pylist(). For very large market catalogs
  (~360k rows), consider batching the insert if memory becomes an issue.
