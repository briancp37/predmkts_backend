# Sprint Progress: Market Metrics
# Release: 03_gold_level
# Created: 2026-02-02

## Status: In Progress

## Completed Tasks
- [2026-02-02] mark_price_computation: Created src/prediction_data/gold/market_marks.py
  - Reads Silver polymarket.trades for a given date via PyIceberg row filter
  - Groups trades by (market_id, outcome_id) where outcome_id = nonusdc_side token ID
  - Computes VWAP (volume-weighted average price) with fallback to last trade price when < 3 trades
  - Computes volume_usd_24h, trades_count_24h, last_trade_price, active_wallets_24h
  - compute_market_marks() orchestrator reads Silver, computes, writes to S3 Gold Parquet
  - 12 unit tests covering VWAP edge cases, grouping, wallet counting, schema validation
  - All 823 tests pass, mypy clean on new code

- [2026-02-02] s3_gold_output: Marked as complete
  - PyArrow schema, daily partitions, idempotent overwrite were already implemented in mark_price_computation
  - Date range backfill now supported via CLI --start-date/--end-date
- [2026-02-02] cli_and_testing: Added `prediction-data gold compute-marks` CLI command
  - Supports --dt YYYY-MM-DD for single day computation
  - Supports --start-date / --end-date for date range backfill (sequential processing)
  - Supports --platform and --dry-run flags
  - Continues on per-day failure, reports summary at end
  - 7 CLI unit tests: single date, date range, conflict detection, missing args, partial failure, custom platform
  - VWAP unit tests (12) were already written in mark_price_computation phase
  - Integration test (CH round-trip) deferred until clickhouse_mirror category is complete
  - All 841 tests pass, mypy clean

- [2026-02-02] liquidity_metric: Implemented trailing 7-day avg daily volume as liquidity proxy
  - Researched open_interest_usd: not feasible without position state tracking
  - Added liquidity_metric (float64) field to MARKET_MARK_DAILY_SCHEMA
  - _read_gold_marks_for_day() reads prior Gold partitions from S3
  - _collect_prior_volumes() gathers up to 6 prior days of volume data
  - attach_liquidity_metric() computes trailing avg and replaces null column
  - Wired into compute_market_marks(): runs after mark computation, before S3 write
  - 4 new unit tests: no-prior-data, with-prior-volumes, multiple-outcomes, schema-preserved
  - All 861 tests pass, mypy clean

## In Progress


## Blockers/Issues


## Notes
- outcome_id uses nonusdc_side (the token ID) from Silver trades to identify which outcome a trade belongs to
- MIN_TRADES_FOR_VWAP = 3; below this threshold, mark_price falls back to last_trade_price
- ClickHouse mirror not yet wired in compute_market_marks (reserved for clickhouse_mirror category)
- CLI command not yet added (reserved for cli_and_testing category)
- liquidity_metric uses trailing 7-day avg daily volume (LIQUIDITY_WINDOW_DAYS=7); open interest not feasible without position state data
- attach_liquidity_metric() is called in compute_market_marks() after marks are computed, before S3 write
- _read_gold_marks_for_day() reads prior days from S3 Gold partitions; gracefully handles missing days
- On cold start (no prior days), liquidity_metric equals current day's volume_usd_24h
