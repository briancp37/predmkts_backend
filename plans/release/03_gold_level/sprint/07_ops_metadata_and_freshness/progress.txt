# Sprint Progress: Ops Metadata and Freshness
# Release: 03_gold_level
# Created: 2026-02-02

## Status: Complete

## Completed Tasks

- [2026-02-02] pipeline_runs: Created ClickHouse DDL (docker/clickhouse/init/011_pipeline_runs.sql) — MergeTree, ORDER BY (stage, started_at), schema matches PRD spec.
- [2026-02-02] pipeline_runs: Created src/prediction_data/gold/ops_metadata.py with start_run(), end_run(), and track_run() context manager. track_run() yields a mutable dict for callers to set rows_written/bytes_written/output_snapshot_id; auto-marks success on clean exit, failed on exception.
- [2026-02-02] pipeline_runs: 6 unit tests in tests/gold/test_ops_metadata.py — covers start, end, success path, failure path, context dict. All passing. mypy clean, ruff clean.

- [2026-02-02] dataset_partitions: Created ClickHouse DDL (docker/clickhouse/init/012_dataset_partitions.sql) — ReplacingMergeTree(written_at), ORDER BY (dataset, partition_day_utc), schema matches PRD spec.
- [2026-02-02] dataset_partitions: Added record_partition(), get_latest_partition(), get_partitions() to src/prediction_data/gold/ops_metadata.py. record_partition() inserts/upserts via ReplacingMergeTree semantics. Query helpers use FINAL for deduplication.
- [2026-02-02] dataset_partitions: 7 new unit tests (TestRecordPartition: 3 tests, TestGetLatestPartition: 2 tests, TestGetPartitions: 2 tests). All 13 tests passing. mypy clean, ruff clean.

- [2026-02-02] dataset_freshness: Created ClickHouse DDL (docker/clickhouse/init/013_dataset_freshness.sql) — ReplacingMergeTree(last_success_at), ORDER BY dataset. Schema: dataset, last_success_at, expected_lag_seconds, actual_lag_seconds, state, last_run_id.
- [2026-02-02] dataset_freshness: Added DEFAULT_SLAS dict, DATASET_FRESHNESS_COLUMNS, compute_freshness_state(), update_freshness(), get_all_freshness(), check_freshness() to ops_metadata.py. Default SLAs: market_mark_daily=300s, wallet_pnl_daily=600s, wallet_mtm_daily=900s, wallet_position_snapshot_daily=900s. State logic: fresh (<=SLA), stale (>SLA and <=2×SLA), broken (>2×SLA or last run failed).
- [2026-02-02] dataset_freshness: Added `prediction-data gold freshness` CLI command to src/prediction_data/cli/gold.py. Displays tabular output: dataset, state, lag, SLA, last success time. Uses check_freshness() which recomputes state at query time.
- [2026-02-02] dataset_freshness: 12 new unit tests (TestComputeFreshnessState: 6, TestUpdateFreshness: 3, TestGetAllFreshness: 1, TestCheckFreshness: 2). All 25 tests passing. mypy clean, ruff clean.

- [2026-02-03] testing: Added 3 integration tests in tests/gold/test_ops_metadata.py (TestOpsMetadataIntegration class):
  - test_gold_processing_emits_all_ops_records: Simulates a Gold compute job using track_run() + record_partition() + update_freshness(). Verifies all 4 inserts occur (start_run, record_partition, update_freshness, end_run) with correct table names and record contents.
  - test_failed_pipeline_records_error: Verifies pipeline failure captures error message in pipeline_runs.
  - test_run_id_links_across_tables: Verifies run_id consistency across pipeline_runs, dataset_partitions, and dataset_freshness tables.

- [2026-02-03] testing: Created tests/gold/test_freshness_cli.py with 4 tests for CLI output verification:
  - test_displays_freshness_data: Verifies tabular output contains header columns (Dataset, State, Lag, SLA, Last Success) and data rows.
  - test_empty_freshness_data: Verifies "No freshness data recorded yet" message.
  - test_handles_missing_last_success: Verifies None timestamps display as "—".
  - test_output_format_alignment: Verifies header/separator/data rows are present and formatted.

- [2026-02-03] testing: All 32 tests passing (28 existing + 3 integration + 4 CLI). mypy clean, ruff clean.

- [2026-02-03] data_quality_metrics: Created ClickHouse DDL (docker/clickhouse/init/014_data_quality_metrics.sql) — MergeTree, ORDER BY (dataset, partition, run_id). Schema: dataset, partition, check_name, status (pass/fail/warn), observed_value, expected, run_id, checked_at.
- [2026-02-03] data_quality_metrics: Added DATA_QUALITY_COLUMNS, record_quality_check(), get_quality_metrics(), get_failed_checks() to ops_metadata.py. Supports recording individual check results and querying by dataset/partition/run_id.
- [2026-02-03] data_quality_metrics: Added run_quality_checks() orchestrator with dataset-specific check helpers:
  - market_mark_daily: non_null_market_id, non_null_outcome_id, mark_price_range [0,1], non_negative_volume
  - wallet_pnl_daily: non_null_wallet, pnl_sanity (|PnL| <= $1M), non_negative_volume, win_loss_consistency
  - wallet_mtm_daily: non_null_wallet, mtm_sanity (|MTM| <= $10M)
  - wallet_position_snapshot_daily: non_null_wallet, non_null_market_id, non_null_outcome_id, position_sanity (|position| <= 1M)
- [2026-02-03] data_quality_metrics: 15 new unit tests (TestRecordQualityCheck: 2, TestGetQualityMetrics: 3, TestGetFailedChecks: 2, TestRunQualityChecksMarketMarks: 3, TestRunQualityChecksWalletPnl: 4, TestRunQualityChecksWalletMtm: 2, TestRunQualityChecksWalletPositions: 3). All 47 tests passing. mypy clean, ruff clean.

## In Progress

- Instrumentation of Gold CLI commands (compute-marks, compute-wallet-metrics) to call record_partition() after writes is deferred to a follow-up pass. The helpers are ready to be wired in.
- Instrumentation of Gold CLI commands (compute-marks, compute-wallet-metrics) to use track_run() is deferred to a follow-up pass. The helpers are ready to be wired in.

## Blockers/Issues


## Notes

- ops_metadata.py uses a two-insert pattern (one at start, one at end) rather than upsert, since pipeline_runs uses MergeTree (not ReplacingMergeTree). The "running" row persists alongside the completion row; queries should filter on status or use ORDER BY ended_at DESC LIMIT 1.
- The track_run() context manager is the recommended interface for callers — handles error capture and status automatically.
