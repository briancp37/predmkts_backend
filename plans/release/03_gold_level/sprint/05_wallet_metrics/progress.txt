# Sprint Progress: Wallet Metrics
# Release: 03_gold_level
# Created: 2026-02-02

## Status: In Progress

## Completed Tasks

### watchlist_table (2026-02-02)
- Created ClickHouse DDL: `docker/clickhouse/init/006_gold_watchlist.sql`
  - ReplacingMergeTree(added_at), ORDER BY wallet_address
  - Schema: wallet_address, added_at, notes, active (Bool)
- Created `src/prediction_data/gold/watchlist.py` with add_wallet, remove_wallet, list_wallets
  - Uses ReplacingMergeTree semantics: remove = insert with active=false
  - list_wallets uses FINAL keyword for dedup at read time
- Added CLI sub-commands under `prediction-data gold watchlist`:
  - `add ADDR [--notes ...]`
  - `remove ADDR`
  - `list [--all]`
- 11 unit + CLI tests in `tests/gold/test_watchlist.py`, all passing
- mypy clean, 834 tests passing (full suite)

### wallet_pnl_daily (2026-02-02)
- Created `src/prediction_data/gold/wallet_pnl.py`
  - Schema: day_utc, wallet, realized_pnl_usd, fees_usd, volume_usd, trades_count, wins, losses
  - `aggregate_pnl_for_day()` groups ledger rows by wallet, sums PnL/fees/volume, counts wins/losses
  - `compute_wallet_pnl()` reads from CH wallet_position_ledger, writes S3 Parquet + CH
  - Sparse output: only emits rows where trades_count > 0
- Created ClickHouse DDL: `docker/clickhouse/init/007_wallet_pnl_daily.sql`
  - ReplacingMergeTree(version), ORDER BY (wallet, day_utc), TTL 90 days
- Added CLI command: `prediction-data gold compute-pnl --dt YYYY-MM-DD [--dry-run]`
  - Supports --start-date/--end-date for range processing
- 15 unit + CLI tests in `tests/gold/test_wallet_pnl.py`, all passing
- mypy clean, 856 tests passing (full suite)

## In Progress


## Blockers/Issues


## Notes
- wallet_pnl_daily depends on wallet_position_ledger which is from sprint 04
- wallet_mtm_daily and wallet_position_snapshot_daily both depend on watchlist (now done) + market marks
