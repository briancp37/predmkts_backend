# Sprint Progress: 09 Monitoring and Status
# Release: 01_bronze_level
# Created: 2026-01-30

## Status: Complete

## Completed Tasks

- [2026-01-30] status_command_group: Created src/prediction_data/cli/status.py with Typer app,
  Platform/Entity enums, PlatformOption/EntityOption annotated types for shared --platform/--entity
  filtering, and format_table() utility for consistent terminal output. Registered status app in
  cli/main.py. Verified `prediction-data status --help` works. All tests pass (378), mypy clean,
  no new ruff errors.

- [2026-01-30] coverage_check: Added `prediction-data status coverage` command to
  src/prediction_data/cli/status.py. Implements --start-date, --end-date, --platform,
  --entity, --bucket options. Lists S3 prefixes per date, reads manifests for run count
  and row_count, flags missing dates as gaps, displays aligned table with status column
  (OK/MISSING) and summary line. Added _date_range, _resolve_filters, _get_coverage_data
  helpers. Tests in tests/test_status_coverage.py (8 tests): full coverage, partial gaps,
  empty range, multiple runs per date, invalid date, start > end, no bucket, default
  all-platforms-entities. All 386 tests pass, mypy clean, ruff clean.

- [2026-01-30] list_runs: Added `prediction-data status runs` command to
  src/prediction_data/cli/status.py. Implements --platform, --entity, --dt, --last,
  --bucket options. Lists S3 manifest keys, downloads and parses each manifest,
  displays table with run_id/platform/entity/dt/row_count/generated_at sorted by
  generated_at descending. Default --last=20; --dt shows all runs for that date
  (ignoring --last). Added _get_runs_data async helper. Tests in
  tests/test_status_runs.py (7 tests): listing/sorting, --last limit, --dt filter
  shows all, no runs, invalid dt, no bucket, cross-platform sorting. All 393 tests
  pass, mypy clean, ruff clean.

- [2026-01-30] show_run: Added `prediction-data status show-run <run_id>` command to
  src/prediction_data/cli/status.py. Searches S3 for manifest keys containing the
  run_id (supports partial prefix matching). Displays full manifest fields: run_id,
  platform, entity, dt, generated_at, row_count. Lists all part files with S3 URIs.
  Shows source metadata: api_base_url, pagination, cursor. Exits with error and
  helpful hint if run_id not found. Added _find_manifest_by_run_id async helper.
  Tests in tests/test_status_show_run.py (6 tests): found run, not found, partial
  prefix match, no bucket, null cursor display, hint message. All 399 tests pass,
  mypy clean, ruff clean.

- [2026-01-30] validate: Added `prediction-data status validate` command to
  src/prediction_data/cli/status.py. Implements --start-date, --end-date, --platform,
  --entity, --bucket options. For each date/platform/entity combo, groups S3 keys by
  run directory, checks each run has a valid parseable manifest, verifies all part files
  referenced in manifests exist in S3, detects orphaned data files (run dirs without
  manifests), and flags empty runs (row_count=0). Reports summary with counts for
  valid runs, invalid manifests, missing parts, orphaned files, empty runs. Exits
  non-zero if validation errors found (invalid manifests, missing parts, or orphaned
  files). Added ValidationResult dataclass and _validate_data async helper. Tests in
  tests/test_status_validate.py (9 tests): valid data, invalid manifest, missing part
  files, orphaned files, empty run, no data, invalid date, no bucket, start > end.
  All 408 tests pass, mypy clean, ruff clean.

- [2026-01-30] testing_and_documentation: Verified all 408 tests pass (pytest -m
  'not integration'), mypy clean (no issues in 23 source files), ruff clean on
  all status source and test files. Tests already exist across 4 files:
  test_status_coverage.py (8 tests), test_status_runs.py (7 tests),
  test_status_show_run.py (7 tests), test_status_validate.py (9 tests) — 31
  total tests covering all status subcommands with mocked S3Client. Added Status
  CLI section to CLAUDE.md with usage examples for coverage, runs, show-run, and
  validate commands. RELEASE.md CLI Contract (section 9) already had status
  commands documented. All PRD categories now pass.

## In Progress

## Blockers/Issues

## Notes

- The status.py module defines the Typer app, shared option types, and table formatter.
  Subsequent subcommands (coverage, runs, show-run, validate) should import from status.py
  and add their commands to the `app` instance there.
