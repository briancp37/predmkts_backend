# Sprint Progress: Scheduled Order Filled
# Release: 01_bronze_level
# Created: 2026-02-04

## Status: Complete

## Completed Tasks

- [ecs_concurrency_guard] Created src/prediction_data/core/ecs_guard.py with async should_skip_concurrent()
  - Reads ECS_CONTAINER_METADATA_URI_V4 env var; returns False immediately when not in ECS (local dev)
  - GETs {metadata_uri}/task to retrieve own TaskARN, Cluster, and Family
  - Calls ecs.list_tasks() and ecs.describe_tasks() to detect sibling tasks with matching command
  - Fail-open: any API error logs warning and returns False (proceed rather than block)
  - 6 unit tests in tests/test_ecs_guard.py covering all cases:
    not-in-ECS, no siblings, matching concurrent, non-matching, ECS API error, metadata endpoint error
  - All 1135 tests pass, mypy clean, ruff clean

- [cli_concurrency_flag] Added --skip-if-concurrent flag to backfill catchup command
  - New Annotated[bool, typer.Option("--skip-if-concurrent")] parameter on backfill_catchup()
  - When set, iterates resolved entities and calls should_skip_concurrent(entity) before the main loop
  - If guard returns True, outputs "SKIP: concurrent task detected for {entity}, exiting" and returns (exit 0)
  - When not set (default), skips the check entirely — local-dev behavior unchanged
  - 2 unit tests in tests/test_backfill.py::TestCatchupCLI:
    test_skip_if_concurrent_guard_returns_true_exits_cleanly,
    test_skip_if_concurrent_guard_returns_false_proceeds
  - All 1137 tests pass, mypy clean (pre-existing issues in silver only), ruff clean for changed files

## In Progress


## Completed Tasks (continued)

- [eventbridge_schedule_update] Replaced polymarket-trades EventBridge schedule with polymarket-order-filled
  - Renamed PolymarketTradesSchedule resource to PolymarketOrderFilledSchedule
  - Updated schedule Name to polymarket-order-filled-${Environment}
  - Updated Description to "Catchup Polymarket order_filled events every 5 minutes"
  - Changed command override to: ["backfill", "catchup", "--platform", "polymarket", "--entity", "order_filled", "--skip-if-concurrent"]
  - Kept rate(5 minutes) schedule expression and retry policy (MaxRetry: 2, MaxAge: 300s)
  - Renamed TradesScheduleExpression parameter to OrderFilledScheduleExpression; updated KalshiTradesSchedule reference
  - Updated ecs-cluster.yaml ExampleRunCommand to use backfill catchup order_filled example
  - Updated tests/test_exit_criteria.py to expect PolymarketOrderFilledSchedule and the new catchup command
  - All 1137 tests pass, mypy clean (pre-existing silver issues only), no new ruff issues

- [iam_permissions] Added ECSConcurrencyGuard IAM policy to ECSTaskRole in iam-ecs-roles.yaml
  - New policy "ECSConcurrencyGuard" with two statements:
    - ListRunningTasks: ecs:ListTasks on Resource '*' with ArnEquals condition scoped to prediction-data-${Environment} cluster
    - DescribeRunningTasks: ecs:DescribeTasks scoped to arn:aws:ecs:${Region}:${AccountId}:task/prediction-data-${Environment}/*
  - No new parameters needed — cluster name constructed from existing Environment parameter
  - Only grants ListTasks and DescribeTasks (no RunTask, StopTask, or other broader ECS permissions)
  - All 1137 tests pass, mypy clean (pre-existing silver issues only), no new ruff issues

- [cloudwatch_monitoring_update] Updated CloudWatch monitoring to track polymarket-order-filled instead of polymarket-trades
  - Renamed PolymarketTradesMetricFilter to PolymarketOrderFilledMetricFilter
  - Updated FilterPattern from '"polymarket-trades" "ingest"' to '"order_filled" "polymarket"' to match catchup log output
  - Updated MetricName from PolymarketTradesRuns to PolymarketOrderFilledRuns
  - Renamed MissingPolymarketTradesAlarm to MissingPolymarketOrderFilledAlarm with updated AlarmName and AlarmDescription
  - Renamed TradesExpectedIntervalSeconds parameter to OrderFilledExpectedIntervalSeconds (updated all references including KalshiTradesAlarm)
  - Updated dashboard: "Polymarket Trades" label changed to "Polymarket Order Filled", metric reference updated
  - Updated dashboard alarm widget ARN from missing-polymarket-trades to missing-polymarket-order-filled
  - Updated Outputs: renamed MissingPolymarketTradesAlarmArn to MissingPolymarketOrderFilledAlarmArn
  - All 1137 tests pass, mypy clean (pre-existing silver issues only), no new ruff issues

- [check_script_and_verification] Updated check_setup_release_01.py to validate specific schedule names
  - Added expected_schedules list with all 6 schedule names (polymarket-order-filled-prod replaces polymarket-trades-prod)
  - Script now validates each expected schedule exists and is ENABLED, rather than just counting >= 4
  - Added explicit check that warns if old polymarket-trades-prod schedule is still present
  - Still expects 6 total schedules (count unchanged, just name updated)
  - All 1137 tests pass, mypy clean (pre-existing silver issues only), no new ruff issues
  - Remaining steps (dry-run, post-deploy verification) are deployment-time tasks

## Blockers/Issues


## Notes

Context: The polymarket-trades EventBridge schedule (rate(5 min), calling `ingest polymarket-trades`)
is being replaced with a polymarket-order-filled schedule that runs the incremental catchup command.
The CLOB API client and `ingest polymarket-trades` CLI command are kept as manual tools but removed
from scheduled execution.

Key design decision: ECS-based concurrency guard chosen over S3 lock files. The guard reads ECS
task metadata to detect if another task with the same command is already running, and skips if so.
This prevents overlapping runs when a catchup takes longer than the 5-minute schedule interval.

Prerequisite work already done in sprint 10 (incremental_catchup):
- `backfill catchup --platform polymarket --entity order_filled` command exists and works
- Timestamp-incremental logic via find_latest_manifest_source() is implemented
- ingest_order_filled() supports since_timestamp parameter

Hotfix already applied in this session (not yet committed):
- Fixed catchup to use timestamp-incremental path for order_filled instead of date-scoped
  (was incorrectly saying "already up to date" when today's partition existed)
