# Sprint Progress: Event Tags
# Release: 04_api_frontend_integration
# Created: 2026-02-08

## Status: In Progress

## Completed Tasks

### 2026-02-08: Bronze Tags Ingestion (COMPLETE)
- Verified Polymarket Gamma API /events endpoint returns tags array
- Confirmed Bronze events ingestion preserves raw tags array in JSONL output
- Tested with sample events containing tags like 'Sports', 'Basketball', 'Crypto', 'Politics'
- Documented tags schema: array of objects with {id, label, slug, forceShow, publishedAt, createdAt, updatedAt, forceHide?, requiresTranslation, isCarousel?}

### 2026-02-08: Silver Tags Normalization (COMPLETE)
- Updated Silver events schema in tables.py to include tags column as ListType(StringType())
- Added normalization logic in normalize.py to extract tag labels from raw event data
- Missing tags handled gracefully (returns None instead of empty array)
- Tag labels extracted into simple string array for easier querying
- Iceberg table schema updated (field 12: tags as list<string>)

**Note for next developer:**
- Run `prediction-data silver process --platform polymarket --entity events --force-reprocess` on ECS to populate tags column for historical data
- Schema evolution in Iceberg should handle the new column automatically

### 2026-02-08: Gold dim_event Tags (COMPLETE)
- Updated DIM_EVENT_SCHEMA in gold/dimensions.py to include tags as pa.list_(pa.string())
- Added tags extraction logic in _silver_to_dim_event() function
- Updated ClickHouse DDL in docker/clickhouse/init/015_dim_event.sql:
  - Added tags Array(String) DEFAULT [] column to CREATE TABLE
  - Added ALTER TABLE to add column for existing tables
- Updated test fixtures in tests/gold/test_dimensions.py to include tags column
- Added new test: test_tags_extracted() to verify tags are properly extracted
- All 11 dim_event-related tests pass

**Note for next developer:**
- After Silver reprocessing on ECS, run `prediction-data gold load-dims` to populate tags in dim_event
- Tags are queryable via ClickHouse `has(tags, 'TagName')` or `arrayContains(tags, 'TagName')`

### 2026-02-08: API Tags Endpoint (COMPLETE)
- Created new tags API module at src/prediction_data/api/tags/
  - __init__.py - exports router
  - schemas.py - EventTagResponse with label, slug, eventCount
  - service.py - get_event_tags() queries ClickHouse using ARRAY JOIN
  - router.py - GET /api/v1/tags endpoint with rate limiting
- Registered router in main.py at /api/v1/tags prefix
- Added OpenAPI tag description for documentation
- Added 5-minute caching in cache.py (CACHE_DURATION_STATIC)
- Created tests/api/test_tags.py with 4 test cases:
  - test_list_tags_success
  - test_list_tags_empty
  - test_list_tags_slug_generation
  - test_list_tags_filters_empty_labels
- All tests pass (frontend: 550, backend API: 52)

**Query used:**
```sql
SELECT tag AS label, COUNT() AS event_count
FROM prediction_gold.dim_event
ARRAY JOIN tags AS tag
WHERE tag != ''
GROUP BY tag
ORDER BY event_count DESC
```

### 2026-02-08: API Tag Filtering (COMPLETE)
- Added includeTags and excludeTags query params to GET /markets/advanced
  - Both params accept comma-separated tag strings
  - includeTags: markets must belong to events with at least one of these tags
  - excludeTags: markets with events containing any of these tags are excluded
- Updated markets/router.py:
  - Replaced single `tags` param with `includeTags` and `excludeTags`
  - Added parsing logic for both comma-separated params
- Updated markets/service.py:
  - Added `include_tags` and `exclude_tags` parameters to get_markets_advanced()
  - Implemented filtering using ClickHouse `hasAny(e.tags, {tags:Array(String)})`
  - For exclusion: `NOT hasAny(e.tags, {exclude_tags:Array(String)})`
- Added same tag filtering to GET /events endpoint:
  - Updated events/router.py with includeTags and excludeTags params
  - Updated events/service.py with include_tags and exclude_tags filtering
- All tests pass (frontend: 550, backend API: 52)
- Type checking passes (mypy: no issues in 8 source files)

**Note for next developer:**
- OpenAPI spec needs regeneration and TypeScript types need update
- Run: `uvicorn prediction_data.api.main:app --reload` then `curl http://localhost:8000/openapi.json > openapi.json`
- Then: `cd web && npm run generate-api`

### 2026-02-08: PostgreSQL Tag Templates (COMPLETE)
- Created TagTemplate SQLAlchemy model in src/prediction_data/db/models/tag_template.py
  - Fields: id (UUID), user_id (FK to users), name (str), include_tags (array), exclude_tags (array), is_default (bool), created_at, updated_at
  - Foreign key to users with CASCADE delete
  - Unique constraint on (user_id, name) to prevent duplicate template names
- Implemented StringArrayType TypeDecorator for cross-database compatibility:
  - Uses PostgreSQL ARRAY(String) in production
  - Falls back to JSON in SQLite for tests
- Added tag_templates relationship to User model
- Exported TagTemplate from db/__init__.py and db/models/__init__.py
- Table auto-created at API startup via Base.metadata.create_all()
- All tests pass (frontend: 550, backend API: 52)
- Type checking passes

**Note for next developer:**
- No Alembic migration needed - tables are auto-created on startup
- For existing production databases, restart the API to create the table

### 2026-02-08: API Tag Templates CRUD (COMPLETE)
- Created src/prediction_data/api/tag_templates/ module:
  - __init__.py - exports router
  - schemas.py - TagTemplateCreate, TagTemplateUpdate, TagTemplateResponse, TagTemplateListResponse
  - service.py - CRUD operations with SQLAlchemy async queries
  - router.py - REST endpoints with rate limiting and auth
- Implemented all CRUD endpoints:
  - POST /api/v1/tag-templates - create template (requires auth)
  - GET /api/v1/tag-templates - list user's templates
  - GET /api/v1/tag-templates/{id} - get single template
  - PUT /api/v1/tag-templates/{id} - update template
  - DELETE /api/v1/tag-templates/{id} - delete template
- Default template logic: setting isDefault=true automatically unsets previous default
- Registered router in main.py with tag-templates prefix
- Added OpenAPI tag description for documentation
- Created tests/api/test_tag_templates.py with 21 test cases:
  - Create: success, minimal, no auth, duplicate name, default unsets previous, empty name fails
  - List: empty, with items, no auth
  - Get: success, not found, no auth
  - Update: success, partial, set default, duplicate name, not found
  - Delete: success, not found, no auth
  - Flow: create-list-update-delete integration test
- All tests pass (frontend: 550, backend API: 73)
- Type checking passes (mypy: no issues)

**Endpoints available:**
- `POST /api/v1/tag-templates` - Create template
- `GET /api/v1/tag-templates` - List templates
- `GET /api/v1/tag-templates/{id}` - Get template by ID
- `PUT /api/v1/tag-templates/{id}` - Update template
- `DELETE /api/v1/tag-templates/{id}` - Delete template

**Note for next developer:**
- OpenAPI spec needs regeneration: `uvicorn prediction_data.api.main:app --reload` then `curl http://localhost:8000/openapi.json > openapi.json`
- TypeScript types need regeneration: `cd web && npm run generate-api`

### 2026-02-08: Frontend Tag Filter Component (COMPLETE)
- Created web/src/components/TagFilter.tsx component with full include/exclude support
- Features implemented:
  - Modal/drawer that slides in from right (consistent with existing TagsModal)
  - Fetches available tags from GET /api/v1/markets/tags via useTags hook
  - Tags displayed as clickable chips with market count badges
  - Three-state toggle: not selected → include (green) → exclude (red) → clear
  - Visual distinction: emerald/green for include, red for exclude
  - Search/filter input for filtering large tag lists
  - Sort toggle: Popular (by marketCount) vs Alphabetical
  - Selection summary showing included/excluded counts
  - Quick clear button on hover for individual tags
  - Clear All and Apply buttons
- Exports:
  - TagFilter component (default and named)
  - TagFilterSelection interface: { includeTags: string[], excludeTags: string[] }
  - TagSelectionState type: 'include' | 'exclude' | null
  - TagFilterProps interface
- All tests pass (frontend: 550)
- Type checking passes (tsc --noEmit: no errors)

**Usage example:**
```tsx
<TagFilter
  isOpen={isOpen}
  onClose={() => setIsOpen(false)}
  includeTags={['Sports', 'Basketball']}
  excludeTags={['Politics']}
  onApply={({ includeTags, excludeTags }) => {
    // Update filter state with new selections
  }}
/>
```

**Note for next developer:**
- The TagFilter component is ready but not yet integrated into the Markets page
- Next sprint item "Frontend Markets Page Integration" will integrate this component
- The existing TagsModal in markets page only supports single-mode (include only)
- TagFilter supports both include and exclude modes which is required for the API params

### 2026-02-08: Frontend Tag Templates UI (COMPLETE)
- Created web/src/hooks/use-tag-templates.ts with React Query hooks:
  - useTagTemplates() - Fetch user's templates with auth check
  - useCreateTagTemplate() - Create with optimistic updates
  - useUpdateTagTemplate() - Update with optimistic updates
  - useDeleteTagTemplate() - Delete with optimistic updates
  - Automatic cache invalidation on mutations
  - Error rollback on mutation failure
- Created web/src/components/TagTemplateManager.tsx with full template management:
  - TemplateDropdown: Selector dropdown with template names, tag counts, default indicator
  - SaveTemplateModal: Modal for creating/editing templates with name, tags summary, default toggle
  - TagTemplateManager: Main component integrating all functionality
- Features implemented:
  - Template selector dropdown to quickly apply saved templates
  - "Save as Template" button (appears when tags are selected and no template is active)
  - Star icon for default template indicator
  - Auto-apply default template on page load
  - Edit button opens modal with current template data
  - Delete with confirmation step (Confirm/Cancel)
  - Optimistic UI updates for all CRUD operations
- Updated hooks/index.ts to export new hooks and types
- All tests pass (frontend: 550)
- Type checking passes (tsc --noEmit: no errors)

**Usage example:**
```tsx
import { TagTemplateManager } from '@/components/TagTemplateManager';
import type { TagFilterSelection } from '@/components/TagFilter';

function MarketsPage() {
  const [selection, setSelection] = useState<TagFilterSelection>({
    includeTags: [],
    excludeTags: [],
  });

  return (
    <TagTemplateManager
      currentSelection={selection}
      onApplyTemplate={setSelection}
      onClearSelection={() => setSelection({ includeTags: [], excludeTags: [] })}
    />
  );
}
```

**Note for next developer:**
- TagTemplateManager is ready to integrate into the Markets page
- Works alongside TagFilter component for full tag filtering experience
- Requires authentication - component returns null when not logged in
- The TemplateDropdown can also be used standalone if needed

### 2026-02-08: Frontend Markets Page Integration (COMPLETE)
- Updated MarketFilterValues interface to support includeTags and excludeTags:
  - Added includeTags: string[] for tags to include (markets must have at least one)
  - Added excludeTags: string[] for tags to exclude (markets must not have any)
  - Marked legacy tags field as @deprecated
  - Updated DEFAULT_FILTER_VALUES with empty arrays for new fields
- Updated useMarketsAdvanced hook (use-markets-advanced.ts):
  - Added includeTags and excludeTags params to UseMarketsAdvancedParams
  - Added URL param building for includeTags and excludeTags
  - Maintained backwards compatibility with legacy tags param
- Updated MarketFilters component (market-filters.tsx):
  - Changed props from selectedTagsCount to includeTagsCount/excludeTagsCount
  - Tags button now shows separate green (+N) and red (-N) count badges
  - Updated hasActiveFilters and More Filters indicator to check new fields
- Updated Markets page (markets/page.tsx):
  - Replaced lazy-loaded TagsModal with TagFilter component
  - Added lazy-loaded TagTemplateManager component
  - Added URL persistence for tag filters using useSearchParams/useRouter
  - Initialized filters from URL params on page load
  - Tag selections sync to URL for shareable links (e.g., /markets?includeTags=Sports,Crypto&excludeTags=Politics)
  - Added handleTagFilterApply and handleApplyTemplate callbacks
  - Added handleClearTagSelection for clearing all tag filters
  - Updated buildApiParams to use new includeTags/excludeTags
  - Legacy tags field support for backwards compatibility
- All tests pass (frontend: 550)
- Type checking passes (tsc --noEmit: no errors)

**Features added:**
- TagFilter modal accessible from More Filters > Tags button
- Include/exclude tag filtering with visual indicators
- TagTemplateManager in header for authenticated users
- URL persistence for shareable filtered views
- Auto-apply default template on page load

**Note for next developer:**
- The "Add tag chips to market cards" step was not implemented in this sprint
  - This would require event tags to be returned in the market data from the API
  - Currently markets only have category, not event tags
  - Consider adding eventTags field to MarketAdvancedResponse if needed

## In Progress

None - all frontend integration complete.

## Remaining

### Testing and Documentation
- Next step: Add tests and update documentation

## Blockers/Issues

None currently.

## Notes

This sprint implements end-to-end tag support:
1. Bronze → Silver → Gold pipeline for event tags
2. API endpoints for tag listing and filtering
3. User-saved tag templates (PostgreSQL)
4. Frontend components for tag filtering and template management

Key decisions made:
- Tags schema: Using Array(String) for simplicity (just tag labels)
- Tag labels extracted from Bronze objects with {label, slug, ...} structure
- Missing tags return None (not empty array) for storage efficiency

Dependencies:
- Requires dim_event populated in ClickHouse
- Requires user auth for tag templates
