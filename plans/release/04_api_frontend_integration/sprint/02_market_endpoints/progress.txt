# Sprint 02: Market Endpoints - Progress

## Status: In Progress

## Completed

### 2026-02-05: ClickHouse Query Utilities
- Created `src/prediction_data/api/clickhouse.py` with:
  - `get_clickhouse_client()`: Singleton pattern for ClickHouse client reuse
  - `execute_query()`: Async wrapper that runs queries in thread pool to avoid blocking
  - `execute_query_count()`: Convenience function for COUNT queries
  - `build_where_clause()`: Builds WHERE clauses with ILIKE support for search fields
  - `build_pagination()`: Builds LIMIT/OFFSET with max limit clamping
  - `build_order_by()`: Builds ORDER BY with field validation
  - `rows_to_models()`: Converts query results to Pydantic models
  - `ping()`: Async health check function
- Default query timeout: 10 seconds
- Thread pool with 4 workers for concurrent query execution
- All type checks pass (mypy)
- All 1441 tests pass (pytest)

### Previously Completed (before this sprint started)
- Markets router structure already exists at `src/prediction_data/api/markets/`
- Market schemas already defined in `schemas.py`:
  - MarketOutcome, MarketBase, MarketResponse, MarketAdvancedResponse
  - MarketListResponse, MarketAdvancedListResponse
  - TradeResponse, PriceHistoryPoint, MarketFilters
- Markets router registered in `api/main.py` with prefix `/api/v1/markets`

### 2026-02-05: GET /markets Endpoint
- Implemented `get_markets()` service function in `src/prediction_data/api/markets/service.py`:
  - Queries ClickHouse dim_market table with LEFT JOIN to dim_event for category filtering
  - Queries dim_outcome for outcome details per market
  - Falls back to parsing tokens JSON if outcomes not in dim_outcome
  - Supports filters: category (exact match via event), search (ILIKE on question/description), resolved (boolean)
  - Returns paginated results ordered by updated_at DESC
  - Helper function `_parse_tokens_to_outcomes()` for extracting outcomes from tokens JSON
- Implemented `GET /markets` endpoint in `src/prediction_data/api/markets/router.py`:
  - Query params: category, search, resolved, limit (1-500, default 50), offset
  - Returns MarketListResponse with items, total, page, limit
  - Full OpenAPI documentation with descriptions
- All type checks pass (mypy)
- All 1441 tests pass (pytest)
- Note: totalVolume, liquidity, currentPrice, endDate, imageUrl fields return default values
  (0.0, 0.5, None) as they are not in current ClickHouse dim_market schema. Will be populated
  when market_mark_daily integration is added or CLOB API client is implemented.

### 2026-02-05: GET /markets/{id} Endpoint
- Implemented `get_market_by_id()` service function in `src/prediction_data/api/markets/service.py`:
  - Queries ClickHouse dim_market with flexible ID matching (platform_market_id OR market_slug)
  - LEFT JOIN to dim_event for category data
  - Queries dim_outcome for outcome details
  - Falls back to parsing tokens JSON if outcomes not in dim_outcome
  - Returns market dict formatted for MarketResponse, or None if not found
- Implemented `GET /markets/{id}` endpoint in `src/prediction_data/api/markets/router.py`:
  - Path parameter: market_id (can be internal ID, polymarketId, or slug)
  - Returns MarketResponse with full outcome details
  - Returns 404 HTTPException if market not found
  - Full OpenAPI documentation with descriptions
- All type checks pass (mypy)
- All 1441 tests pass (pytest)
- Note: CLOB integration (include_clob param) deferred to CLOB API Client implementation

### 2026-02-05: GET /markets/{id}/trades Endpoint
- Implemented `get_market_trades()` service function in `src/prediction_data/api/markets/service.py`:
  - Queries ClickHouse wallet_position_ledger table for trades by market_id
  - LEFT JOIN to dim_outcome for outcome labels
  - Supports filters: startDate, endDate (YYYY-MM-DD format)
  - Returns paginated results ordered by timestamp DESC (most recent first)
  - Generates synthetic trade IDs from market_id + wallet + timestamp
  - Maps BUY/SELL side from ledger data
  - Calculates usdValue from qty * price
- Implemented `GET /markets/{id}/trades` endpoint in `src/prediction_data/api/markets/router.py`:
  - Path parameter: market_id (resolved via get_market_by_id for flexible ID matching)
  - Query params: limit (1-1000, default 100), offset, startDate, endDate
  - Returns TradeListResponse with items, total, page, limit
  - Returns 404 HTTPException if market not found
  - Full OpenAPI documentation with descriptions
- Updated schemas.py:
  - Made TradeResponse.txHash optional (str | None) since wallet_position_ledger doesn't contain transaction hashes
  - Added TradeListResponse schema for paginated trade responses
- All type checks pass (mypy)
- All 1441 tests pass (pytest)
- Note: txHash field returns None as the wallet_position_ledger table doesn't store transaction hashes.
  Full txHash support would require querying the Silver Iceberg trades table directly.

### 2026-02-05: GET /markets/{id}/price-history Endpoint
- Implemented `get_market_price_history()` service function in `src/prediction_data/api/markets/service.py`:
  - Queries ClickHouse market_mark_daily table for historical price data
  - Supports interval param: 1d (daily), 1w (weekly aggregation)
  - Note: 1h and 4h intervals return daily data since hourly data is not yet available
  - Supports filters: startDate, endDate (YYYY-MM-DD format)
  - Optional outcomeId parameter; defaults to YES outcome if not specified
  - Auto-selects YES outcome by querying dim_outcome for side='Yes' or label containing 'yes'
  - Returns chronological order (oldest first) for chart rendering
  - Weekly interval uses toMonday() for aggregation with sum(volume) and argMax(price)
- Implemented `GET /markets/{id}/price-history` endpoint in `src/prediction_data/api/markets/router.py`:
  - Path parameter: market_id (resolved via get_market_by_id for flexible ID matching)
  - Query params: interval (1h|4h|1d|1w, default 1d), startDate, endDate, outcomeId
  - Returns PriceHistoryResponse with items, marketId, outcomeId, interval, startDate, endDate
  - Returns 404 HTTPException if market not found
  - Full OpenAPI documentation with descriptions
- Added `PriceHistoryResponse` schema to schemas.py for structured response metadata
- All type checks pass (mypy)
- All 1441 tests pass (pytest)
- Note: Sub-daily intervals (1h, 4h) return daily granularity as hourly data is not yet computed.
  Future enhancement: compute hourly marks from Silver trades for finer-grained charting.

## In Progress

## Blockers

## Notes
- The ClickHouse client uses clickhouse_connect library (same as Gold layer)
- Query execution is wrapped in ThreadPoolExecutor to provide async interface
- The build_where_clause function supports parameterized queries to prevent SQL injection
- Next priority: Implement CLOB API client for real-time prices, or Events Router Setup

## Key Files to Create/Modify
- src/prediction_data/api/markets/router.py
- src/prediction_data/api/markets/service.py
- src/prediction_data/api/markets/schemas.py
- src/prediction_data/api/events/router.py
- src/prediction_data/api/events/service.py
- src/prediction_data/api/events/schemas.py
- src/prediction_data/api/tags/router.py (or add to markets)
- src/prediction_data/api/clickhouse.py [DONE]
- src/prediction_data/api/clob_client.py
- src/prediction_data/api/main.py (register new routers)
- web/src/hooks/useMarkets.ts
- web/src/hooks/useMarketsAdvanced.ts

## ClickHouse Tables to Query
- dim_market (market metadata)
- dim_outcome (outcome details)
- dim_event (event metadata)
- market_mark_daily (price history, VWAP)
- wallet_position_ledger (trades)

## External APIs
- Polymarket CLOB API: https://clob.polymarket.com/book?token_id={token_id}
  - Rate limit: 500 req/10s
  - Response: order book with bids/asks arrays

## Frontend Pages Dependent on This Sprint
- /(dashboard)/markets - Market listing with filters
- /(dashboard)/analyze-market - Market detail page
- /(dashboard)/screener - Market screener
