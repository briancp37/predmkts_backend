# Sprint Progress: Kalshi Integration
# Release: 01_bronze_level
# Created: 2026-01-27

## Status: In Progress

## Completed Tasks

### 2026-01-28: API Research (api_research)
- Researched Kalshi API documentation and endpoints
- Created comprehensive README.md at src/prediction_data/bronze/kalshi/README.md
- Documented:
  - API base URLs: Production (api.elections.kalshi.com/trade-api/v2) and Demo (demo-api.kalshi.co/trade-api/v2)
  - Authentication: RSA-PSS signature-based auth with KALSHI-ACCESS-KEY, KALSHI-ACCESS-SIGNATURE, KALSHI-ACCESS-TIMESTAMP headers
  - Trades endpoint: GET /markets/trades with cursor pagination, filtering by ticker and timestamp
  - Markets endpoint: GET /markets with cursor pagination, filtering by status, timestamps, tickers
  - Rate limits by tier: Basic (20/10 r/w per sec), Advanced (30/30), Premier (100/100), Prime (400/400)
  - Pagination pattern: cursor-based with limit up to 1000
  - Response schemas for Trade and Market objects
- All tests passing (139 tests), typecheck clean

### 2026-01-28: Authentication Implementation (authentication)
- Added Kalshi API configuration to Settings class in src/prediction_data/core/config.py:
  - kalshi_api_key_id: Kalshi API key ID (optional, loaded from KALSHI_API_KEY_ID env var)
  - kalshi_private_key_path: Path to RSA private key PEM file (optional, loaded from KALSHI_PRIVATE_KEY_PATH)
  - kalshi_api_base_url: Kalshi API base URL (defaults to production)
- Created src/prediction_data/bronze/kalshi/auth.py with:
  - KalshiAuthError: Custom exception for authentication failures
  - KalshiCredentials: Frozen dataclass holding api_key_id and RSA private_key
  - load_private_key(): Load RSA private key from PEM file
  - load_credentials_from_settings(): Load credentials from environment variables
  - sign_request(): Generate RSA-PSS signature for API requests
  - generate_auth_headers(): Generate all three required auth headers
- Added cryptography>=42.0.0 dependency to pyproject.toml
- Created comprehensive test suite in tests/test_kalshi_auth.py (18 tests)
- Updated src/prediction_data/bronze/kalshi/__init__.py with exports
- All tests passing (184 tests), typecheck clean
- Note: Kalshi doesn't require token refresh - signatures are generated per-request

### 2026-01-28: API Client Implementation (api_client)
- Created src/prediction_data/bronze/kalshi/client.py with KalshiClient class
- Implemented KalshiClient as async context manager following PolymarketClient patterns
- Key features implemented:
  - Cursor-based pagination for both trades and markets endpoints
  - fetch_trades_page() and fetch_all_trades() methods with filtering support
  - fetch_markets_page() and fetch_all_markets() methods with filtering support
  - Authentication headers generated per-request via generate_auth_headers()
  - Integration with shared HttpClient from core infrastructure
  - Structured logging for pagination progress and record counts
  - Configurable page size (default 100, max 1000)
  - Optional base_url for switching between production and demo APIs
- Updated src/prediction_data/bronze/kalshi/__init__.py with new exports:
  - KalshiClient, KALSHI_API_BASE_URL, KALSHI_DEMO_API_BASE_URL
  - DEFAULT_PAGE_SIZE, MAX_PAGE_SIZE constants
- Created comprehensive test suite in tests/test_kalshi_client.py (27 tests)
- All tests passing (211 tests), typecheck clean

### 2026-01-28: Trades Ingestion Implementation (trades_ingestion)
- Created src/prediction_data/bronze/kalshi/ingest.py with:
  - ingest_trades(): Async function that fetches trades and uploads to S3
  - run_ingest_trades(): Synchronous wrapper for CLI usage
  - Integration with RunContext for tracking run_id and duration
  - Support for filtering by ticker, min_ts, max_ts
  - Manifest creation with correct platform, entity, and cursor pagination metadata
- Added CLI command `prediction-data ingest kalshi-trades` with options:
  - --dt (required): Data partition date in YYYY-MM-DD format
  - --bucket: S3 bucket name (defaults to BRONZE_BUCKET env var)
  - --ticker: Market ticker to filter by
  - --min-ts: Minimum Unix timestamp filter
  - --max-ts: Maximum Unix timestamp filter
- Updated src/prediction_data/bronze/kalshi/__init__.py with new exports:
  - ingest_trades, run_ingest_trades
- Created comprehensive test suite in tests/test_kalshi_ingest.py (6 tests)
- All tests passing (217 tests), typecheck clean

### 2026-01-28: Markets Ingestion Implementation (markets_ingestion)
- Extended src/prediction_data/bronze/kalshi/ingest.py with:
  - ingest_markets(): Async function that fetches all markets snapshot and uploads to S3
  - run_ingest_markets(): Synchronous wrapper for CLI usage
  - Integration with RunContext for tracking run_id and duration
  - Support for filtering by event_ticker, series_ticker, status
  - Manifest creation with correct platform, entity, and cursor pagination metadata
- Added CLI command `prediction-data ingest kalshi-markets` with options:
  - --dt (required): Data partition date in YYYY-MM-DD format
  - --bucket: S3 bucket name (defaults to BRONZE_BUCKET env var)
  - --event-ticker: Event ticker to filter by
  - --series-ticker: Series ticker to filter by
  - --status: Market status filter (unopened, open, paused, closed, settled)
- Updated src/prediction_data/bronze/kalshi/__init__.py with new exports:
  - ingest_markets, run_ingest_markets
- Added 6 new tests to tests/test_kalshi_ingest.py (now 12 tests total)
- All tests passing (223 tests), typecheck clean

## In Progress


## Blockers/Issues


## Notes
- Kalshi uses RSA-PSS signatures (not simple API keys) - requires private key PEM file
- Signature = sign(timestamp_ms + HTTP_METHOD + path_without_query_params)
- Environment variables needed: KALSHI_API_KEY_ID, KALSHI_PRIVATE_KEY_PATH
- Maximum limit per request is 1000 records
- Cursor-based pagination (empty cursor = end of results)
